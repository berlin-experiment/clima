import 'package:flutter/material.dart';

class ExpandedFlexTwo extends StatelessWidget {
  const ExpandedFlexTwo({
    Key? key,
  }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Expanded(flex: 1, child: Container());
  }
}
